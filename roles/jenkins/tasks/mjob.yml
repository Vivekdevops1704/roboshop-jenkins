- name: copy config file - {{ item.job_name }}
  ansible.builtin.template:
    src: mjob.xml
    dest: /tmp/mjob.xml

- name: Create job - {{ item.job_name }}
  community.general.jenkins_job:
    config: "{{ lookup('file', '/tmp/mjob.xml') }}"
    name: "{{ item.job_name }}"
    password: admin123
    url: http://54.162.108.112:8080
    user: admin
